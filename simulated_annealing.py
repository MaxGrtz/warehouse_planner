import algorithm

class Simulated_Annealing(algorithm.Algorithm):
    
    def __init__(self, psu_dict, order, decode_dict):
        super().__init__(psu_dict, order, decode_dict)
        self.name = "Simulated Annealing"


    def run(self):
        

        return "3", "simulated annealing"
